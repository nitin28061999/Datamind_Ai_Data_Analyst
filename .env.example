# Required — get a free key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Optional — leave blank to use the local fallback prompt.
# This pipe already exists: https://langbase.com/nitin28061999/datamind-ai-analyst
# Get its key from that pipe's page -> Settings -> API keys (a PIPE key, not
# your general account key) and paste it below.
LANGBASE_API_KEY=
LANGBASE_PIPE_URL=https://api.langbase.com/v1/pipes/run
